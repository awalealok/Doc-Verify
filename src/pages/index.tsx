import React from 'react';
import App from './_app';

export default function HomePage() {
  // Render the app UI as a Next.js page. _app.tsx contains the app component used here.
  return <App />;
}
